# Fish completion for Claude Code CLI
# Installed by the claude-code DevContainer Feature.
# To regenerate from your installed version:
#   claude completions fish > /usr/share/fish/vendor_completions.d/claude.fish

# Subcommands
complete -c claude -n "not __fish_seen_subcommand_from agents auth auto-mode doctor install mcp plugin plugins setup-token update upgrade" -a agents -d "List available agents"
complete -c claude -n "not __fish_seen_subcommand_from agents auth auto-mode doctor install mcp plugin plugins setup-token update upgrade" -a auth -d "Manage authentication"
complete -c claude -n "not __fish_seen_subcommand_from agents auth auto-mode doctor install mcp plugin plugins setup-token update upgrade" -a auto-mode -d "Toggle automatic mode"
complete -c claude -n "not __fish_seen_subcommand_from agents auth auto-mode doctor install mcp plugin plugins setup-token update upgrade" -a doctor -d "Check installation health"
complete -c claude -n "not __fish_seen_subcommand_from agents auth auto-mode doctor install mcp plugin plugins setup-token update upgrade" -a install -d "Install components"
complete -c claude -n "not __fish_seen_subcommand_from agents auth auto-mode doctor install mcp plugin plugins setup-token update upgrade" -a mcp -d "Manage MCP servers"
complete -c claude -n "not __fish_seen_subcommand_from agents auth auto-mode doctor install mcp plugin plugins setup-token update upgrade" -a plugin -d "Manage plugins"
complete -c claude -n "not __fish_seen_subcommand_from agents auth auto-mode doctor install mcp plugin plugins setup-token update upgrade" -a plugins -d "List plugins"
complete -c claude -n "not __fish_seen_subcommand_from agents auth auto-mode doctor install mcp plugin plugins setup-token update upgrade" -a setup-token -d "Configure authentication token"
complete -c claude -n "not __fish_seen_subcommand_from agents auth auto-mode doctor install mcp plugin plugins setup-token update upgrade" -a update -d "Update Claude Code"
complete -c claude -n "not __fish_seen_subcommand_from agents auth auto-mode doctor install mcp plugin plugins setup-token update upgrade" -a upgrade -d "Upgrade Claude Code"

# Flags
complete -c claude -s h -l help -d "Show help"
complete -c claude -s v -l version -d "Show version"
complete -c claude -s p -l print -d "Print response to stdout"
complete -c claude -s c -l continue -d "Continue previous conversation"
complete -c claude -s r -l resume -d "Resume a specific conversation"
complete -c claude -s d -l debug -d "Enable debug mode"
complete -c claude -s n -l name -x -d "Name for the conversation"
complete -c claude -s w -l worktree -d "Use git worktree"
complete -c claude -l model -x -d "Model to use"
complete -c claude -l effort -x -d "Effort level"
complete -c claude -l bare -d "Bare output mode"
complete -c claude -l verbose -d "Verbose output"
complete -c claude -l json-schema -x -d "JSON schema for output"
complete -c claude -l max-budget-usd -x -d "Maximum budget in USD"
complete -c claude -l output-format -x -a "text json stream-json" -d "Output format"
complete -c claude -l input-format -x -a "text json" -d "Input format"
complete -c claude -l permission-mode -x -a "auto ask deny" -d "Permission mode"
complete -c claude -l system-prompt -x -d "System prompt"
complete -c claude -l append-system-prompt -x -d "Append to system prompt"
complete -c claude -l add-dir -r -d "Add directory to context"
complete -c claude -l allowed-tools -x -d "Allowed tools"
complete -c claude -l disallowed-tools -x -d "Disallowed tools"
complete -c claude -l tools -x -d "Tools configuration"
complete -c claude -l mcp-config -r -d "MCP configuration file"
complete -c claude -l settings -r -d "Settings file"
complete -c claude -l ide -x -d "IDE integration"
complete -c claude -l tmux -d "Run in tmux session"
complete -c claude -l file -r -d "Input file"
complete -c claude -l agent -x -d "Agent to use"
complete -c claude -l agents -d "List agents"
complete -c claude -l betas -x -d "Enable beta features"
complete -c claude -l chrome -d "Enable Chrome integration"
complete -c claude -l no-chrome -d "Disable Chrome integration"
